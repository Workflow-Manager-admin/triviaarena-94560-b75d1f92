# Core dependencies for QuizRealm FastAPI backend

fastapi==0.115.12
uvicorn==0.34.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
pydantic==2.11.3
sqlalchemy==2.0.29
python-dotenv==1.1.0
python-multipart==0.0.20
email-validator==2.2.0
# starlette and websockets are pulled in by fastapi, but can be pinned for compatibility
starlette==0.46.1
websockets==15.0.1

# The following are convenience/dev/test/cli tools, not strictly needed for runtime, but often present:
# flake8==7.2.0
# pytest==8.3.5
# fastapi-cli==0.0.7
# rich==14.0.0

# Note: sqlite3 is a Python standard library module, so no install needed.
